# Multi-threaded-TCP-Client-Server-Application-Distributed-Systems-
• Built a TCP socket server handling concurrent clients via multi-threading, with a client that exchanges requests over a defined message protocol in Java • Handled connection failures, invalid input and synchronisation of shared state to keep the server reliable under concurrent load
